# Auth Module Configuration

# Server
server.host=0.0.0.0
server.port=8001

# Database
database.url=sqlite:///database/finance_tracker.db

# CORS
cors.allowed_origins=http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:8000

# Security
security.secret_key=your-secret-key-here
security.algorithm=HS256
security.access_token_expire_minutes=30

# Environment
environment=development
